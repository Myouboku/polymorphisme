public class Enseignant_Permanent extends Personne {
    public Enseignant_Permanent(String nom, String prenom, Enseignement ens) {
        // appel au constructeur de la super classe
        super(nom, prenom, ens);
    }
}
